# Experiment configuration for the SBPO 2025 Wave Order-Picking Challenge

# Dataset to run experiments on
instance_dir: datasets/a

# Number of repetitions per (algorithm, instance) pair
repetitions: 1

# Base seed for reproducibility (run i uses seed_base + i)
seed_base: 42

# Maximum number of parallel workers (null = cpu_count - 1)
max_workers: null

# Time limit per solver run in seconds (null = no limit)
time_limit: 300

# Output directory for results
result_dir: results

# Save problem output (selected orders and aisles) to CSV (default: false)
save_output: True

algorithms:
  - name: af_useful_order-desc_prune-multi
    algo: aisle_first
    deterministic: true
    params:
      score: useful
      order: desc
      prune: multi
  
  # - name: bigger_multi
  #   algo: simple
  #   deterministic: True
  #   params:
  #     order: desc
  #     greedy: multi

  # - name: ga_base_uniform_seeded_random
  #   algo: ga
  #   params:
  #     variant: BaseGA
  #     pop_size: 50
  #     epoch: 300
  #     pc: 0.9
  #     pm: 0.05
  #     selection: tournament
  #     crossover: uniform
  #     mutation: flip
  #     k_way: 0.2
  #     aisle_selector: multi
  #     seed_with_heuristics: true
  #     start: random

  # - name: ga_base_uniform_seeded_aisle
  #   algo: ga
  #   params:
  #     variant: BaseGA
  #     pop_size: 50
  #     epoch: 300
  #     pc: 0.9
  #     pm: 0.05
  #     selection: tournament
  #     crossover: uniform
  #     mutation: flip
  #     k_way: 0.2
  #     aisle_selector: multi
  #     seed_with_heuristics: true
  #     start: seed_aisle

  - name: ga_base_uniform_seeded_useful_aisle
    algo: ga
    params:
      variant: BaseGA
      pop_size: 50
      epoch: 300
      pc: 0.9
      pm: 0.05
      selection: tournament
      crossover: uniform
      mutation: flip
      k_way: 0.2
      aisle_selector: multi
      seed_with_heuristics: true
      start: seed_aisle

  - name: ga_full
    algo: ga_full
    params:
      variant: BaseGA
      pop_size: 50
      epoch: 300
      pc: 0.9
      pm: 0.05
      selection: tournament
      crossover: uniform
      mutation: flip
      k_way: 0.2
      aisle_selector: multi
      seed_with_heuristics: true
      start: useful_seed_aisle

  # - name: ga_full_25
  #   algo: ga_full
  #   params:
  #     variant: BaseGA
  #     pop_size: 25
  #     epoch: 300
  #     pc: 0.9
  #     pm: 0.05
  #     selection: tournament
  #     crossover: uniform
  #     mutation: flip
  #     k_way: 0.2
  #     aisle_selector: multi
  #     seed_with_heuristics: true
  #     start: useful_seed_aisle